# BÁO CÁO CÁ NHÂN (REFLECTION) - SPRINT 2
**Vị trí:** AI/Backend Engineer
**Họ và tên:** Nguyễn Bá Hào - 2A202600133

## 1. Các công việc đã hoàn thành (Thực tế)

### 1.1. Cấu trúc Multi-Judge Consensus (`engine/llm_judge.py`)
Tôi đã hoàn thiện logic chấm điểm kết hợp giữa 2 nhà cung cấp khác nhau:
*   **Mô hình sử dụng:** Đã cấu hình gọi **Gemma 4 (Google)** và **GPT-4o/mini (OpenAI)** song song.
*   **Logic tính toán:** Cài đặt công thức tính `agreement_rate` (độ đồng thuận) và `final_score` (điểm trung bình).
*   **Quản lý chi phí:** Đã viết code tính toán `cost` dựa trên token thực tế (OpenAI) và giá định mức (Gemma) để kiểm soát ngân sách.

### 1.2. Tối ưu hóa Runner (`engine/runner.py`)
*   **Lập trình bất đồng bộ:** Chuyển đổi Runner sang kiến trúc `asyncio` với cơ chế `batch_size` để có thể chạy nhiều test case cùng lúc.
*   **Tổng hợp chỉ số:** Viết hàm `get_summary_metrics` để trích xuất các thông số Latency, Cost, và Judge Score ngay sau khi kết thúc benchmark.

### 1.3. Xử lý tương thích hệ thống (System Fixes)
Đây là phần tốn nhiều thời gian nhất do sự khác biệt về phiên bản Python giữa máy phát triển và môi trường thực tế:
*   **Sửa lỗi Version:** Thay thế toàn bộ `datetime.UTC` thành `datetime.timezone.utc` trong 5 file (`main_agent.py`, `retrieval.py`, `synthesis.py`, `policy_tool.py`, `retrieval_eval.py`) để code chạy được trên Python 3.10.
*   **Quản lý thư viện:** Cập nhật `requirements.txt` sau khi phát hiện thiếu các thư viện quan trọng như `litellm`, `google-genai`, `pyyaml`.

## 2. Tình trạng hiện tại (Status Update)
*   **Phần Logic:** Đã hoàn thiện 100% cấu trúc code cho Judge và Runner.
*   **Phần Thực thi:** Đã hoàn thành chạy thử nghiệm 5 case mẫu để kiểm chứng logic chấm điểm.

## 3. Khó khăn và Bài học kinh nghiệm
*   **Sự phụ thuộc thư viện:** Bài học lớn nhất là việc sử dụng các framework như Ragas kéo theo rất nhiều thư viện phụ trợ (Langchain, Datasets). Cần kiểm tra kỹ `requirements.txt` trước khi triển khai.
*   **Lập trình Async:** Việc gọi nhiều API cùng lúc dễ dẫn đến lỗi nếu không có cơ chế `try...except` chặt chẽ cho từng task lẻ (đã được bổ sung vào `runner.py`).
*   **Tính nhất quán:** Việc bị ghi đè file trong quá trình làm việc yêu cầu phải kiểm tra lại cấu trúc class (`RetrievalEvaluator`) để đảm bảo không mất logic cũ của Member 1.

## 4. Hành động tiếp theo
*   Chạy thử nghiệm 30 case mẫu để kiểm chứng logic chấm điểm trước khi chạy toàn bộ 50 case.

---
**Người báo cáo:** Nguyễn Bá Hào
